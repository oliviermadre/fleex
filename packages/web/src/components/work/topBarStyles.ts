/**
 * Shared look of the Work top bar's group labels (MODE, PINNED, TICKET) — the
 * small caps that name each cluster. Kept here rather than in one of the bar's
 * components so the clusters can't drift apart.
 */
export const GROUP_LABEL = 'text-[9.5px] font-semibold tracking-[0.08em] text-[var(--theme-text-faint)]';
